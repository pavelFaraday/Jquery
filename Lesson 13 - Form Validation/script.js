$(document).ready(function () {
    $('input:text').val();  // get value
    $('input:text').val('world'); // set Value

    $('input:password').val('123456'); // set Value
    $('input:password').val(); // get value
});